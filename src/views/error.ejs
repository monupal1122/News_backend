<%- include('partials/header') %>

    <div class="container py-5 mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6 text-center">
                <div class="display-1 text-danger mb-4">
                    <i class="bi bi-exclamation-triangle-fill"></i>
                </div>
                <h1 class="fw-bold">
                    <%= typeof message !=='undefined' ? message : 'Something went wrong!' %>
                </h1>
                <p class="text-muted mb-4">We apologize for the inconvenience. Please try again or go back to the
                    dashboard.</p>
                <a href="/admin/dashboard" class="btn btn-primary px-4 py-2">
                    <i class="bi bi-house-door me-2"></i>Back to Dashboard
                </a>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>